<!doctype html>
<html lang="fr">
  <head>
    <title>Accueil — Website FiveM</title>
    <%- include("../partials/head") %>
  </head>

  <body class="min-h-screen bg-gray-950 text-gray-100">
    <%- include("../partials/nav", { user }) %>

    <!-- Background vibe -->
    <div class="relative">
      <div class="absolute inset-0">
        <div class="h-72 bg-gradient-to-b from-indigo-600/20 to-transparent"></div>
      </div>

      <main class="relative max-w-6xl mx-auto px-4 sm:px-6 py-10">
        <!-- Hero -->
        <section class="rounded-2xl border border-white/10 bg-white/5 backdrop-blur shadow-2xl p-6 sm:p-10">
          <div class="flex flex-col lg:flex-row lg:items-center lg:justify-between gap-8">
            <div class="max-w-2xl">
              <div class="inline-flex items-center gap-2 text-xs font-semibold tracking-wide px-3 py-1 rounded-full bg-indigo-500/10 border border-indigo-400/20 text-indigo-200">
                <span class="w-2 h-2 rounded-full bg-indigo-400"></span>
                Website FiveM • Discord OAuth • MySQL
              </div>

              <h1 class="mt-4 text-3xl sm:text-4xl font-extrabold leading-tight">
                Un dashboard web moderne
                <span class="text-indigo-300">connecté à ton serveur FiveM</span>
              </h1>

              <p class="mt-4 text-gray-300 leading-relaxed">
                Connexion Discord → liaison avec ton compte FiveM, sans toucher à
                <code class="px-2 py-1 rounded bg-black/40 border border-white/10 text-gray-200">users.identifier</code>.
                Simple, propre, et fait pour évoluer (stats, boutique, whitelist…).
              </p>

              <!-- Actions -->
              <div class="mt-6 flex flex-col sm:flex-row sm:flex-wrap gap-3">
                <% if (!user) { %>
                  <a href="/login"
                     class="inline-flex items-center justify-center px-4 py-2.5 rounded-xl bg-indigo-600 hover:bg-indigo-500 text-white font-semibold shadow-lg shadow-indigo-600/20 transition">
                    Se connecter avec Discord
                  </a>

                  <a href="/dashboard"
                     class="inline-flex items-center justify-center px-4 py-2.5 rounded-xl bg-white/5 hover:bg-white/10 border border-white/10 text-gray-100 transition">
                    Voir le dashboard
                  </a>
                <% } else { %>
                  <a href="/dashboard"
                     class="inline-flex items-center justify-center px-4 py-2.5 rounded-xl bg-white/5 hover:bg-white/10 border border-white/10 text-gray-100 transition">
                    Dashboard
                  </a>

                  <% if (user.linked === true) { %>
                    <form method="POST" action="/unlink" class="inline">
                      <button
                        type="submit"
                        class="inline-flex items-center justify-center px-4 py-2.5 rounded-xl bg-red-600 hover:bg-red-500 text-white font-semibold shadow-lg shadow-red-600/20 transition"
                        onclick="return confirm('Es-tu sûr de vouloir délier ton compte ?');">
                        Délier mon compte
                      </button>
                    </form>
                  <% } else { %>
                    <a href="/link"
                       class="inline-flex items-center justify-center px-4 py-2.5 rounded-xl bg-indigo-600 hover:bg-indigo-500 text-white font-semibold shadow-lg shadow-indigo-600/20 transition">
                      Lier mon compte
                    </a>
                  <% } %>

                  <a href="/logout"
                     class="inline-flex items-center justify-center px-4 py-2.5 rounded-xl bg-white/5 hover:bg-white/10 border border-white/10 text-gray-100 transition">
                    Déconnexion
                  </a>
                <% } %>
              </div>

              <!-- Status -->
              <div class="mt-6">
                <% if (!user) { %>
                  <div class="text-sm text-gray-400">
                    Statut : <span class="text-gray-200 font-semibold">Non connecté</span>
                  </div>
                <% } else { %>
                  <div class="flex flex-col sm:flex-row sm:items-center gap-2 text-sm text-gray-400">
                    <div>
                      Statut : <span class="text-gray-200 font-semibold">Connecté</span>
                    </div>
                    <div class="hidden sm:block text-gray-600">•</div>
                    <div>
                      Liaison : 
                      <% if (user.linked === true) { %>
                        <span class="text-emerald-300 font-semibold">OK</span>
                      <% } else { %>
                        <span class="text-yellow-300 font-semibold">À faire</span>
                      <% } %>
                    </div>
                  </div>
                <% } %>
              </div>
            </div>

            <!-- Card preview -->
            <div class="w-full lg:w-[420px]">
              <div class="rounded-2xl border border-white/10 bg-black/30 p-5 sm:p-6">
                <div class="flex items-center justify-between">
                  <div class="text-sm font-semibold text-gray-200">Aperçu</div>
                  <div class="text-xs text-gray-400">Responsive • Dark</div>
                </div>

                <div class="mt-4 rounded-xl bg-white/5 border border-white/10 p-4">
                  <div class="flex items-center gap-3">
                    <div class="w-12 h-12 rounded-full bg-white/10 border border-white/10 flex items-center justify-center overflow-hidden">
                      <% if (user && user.avatar) { %>
                        <img class="w-full h-full object-cover" src="https://cdn.discordapp.com/avatars/<%= user.discord_id %>/<%= user.avatar %>.png" />
                      <% } else { %>
                        <span class="text-gray-400 text-sm">?</span>
                      <% } %>
                    </div>
                    <div>
                      <div class="text-gray-100 font-semibold">
                        <%= (user && user.username) ? user.username : "Utilisateur" %>
                      </div>
                      <div class="text-xs text-gray-400">
                        <%= (user && user.discord_id) ? ("discord_id: " + user.discord_id) : "connecte-toi pour afficher ton profil" %>
                      </div>
                    </div>
                  </div>

                  <div class="mt-4 grid grid-cols-2 gap-3">
                    <div class="rounded-lg bg-black/30 border border-white/10 p-3">
                      <div class="text-xs text-gray-400">Auth</div>
                      <div class="mt-1 font-semibold">
                        <%= user ? "OK" : "—" %>
                      </div>
                    </div>
                    <div class="rounded-lg bg-black/30 border border-white/10 p-3">
                      <div class="text-xs text-gray-400">Link</div>
                      <div class="mt-1 font-semibold">
                        <%= (user && user.linked === true) ? "OK" : "—" %>
                      </div>
                    </div>
                  </div>

                  <p class="mt-4 text-xs text-gray-400 leading-relaxed">
                    Tu peux brancher ici des stats serveur, des infos whitelist, un shop, etc.
                  </p>
                </div>

                <div class="mt-4 text-xs text-gray-500">
                  Tailwind via cdnjs • thème sombre • design “glass”
                </div>
              </div>
            </div>
          </div>
        </section>

        <!-- Info strip -->
        <section class="mt-6 grid grid-cols-1 md:grid-cols-3 gap-4">
          <div class="rounded-2xl border border-white/10 bg-white/5 p-5">
            <div class="text-sm font-semibold text-gray-200">Respect de la DB</div>
            <p class="mt-2 text-sm text-gray-400">
              On ajoute uniquement des colonnes Discord dans <code class="px-1 rounded bg-black/30 border border-white/10">users</code>.
              <span class="text-gray-300">Identifier reste in-game.</span>
            </p>
          </div>

          <div class="rounded-2xl border border-white/10 bg-white/5 p-5">
            <div class="text-sm font-semibold text-gray-200">Flow clair</div>
            <p class="mt-2 text-sm text-gray-400">
              Login → si lié dashboard, sinon page link. Simple à maintenir.
            </p>
          </div>

          <div class="rounded-2xl border border-white/10 bg-white/5 p-5">
            <div class="text-sm font-semibold text-gray-200">Prêt à étendre</div>
            <p class="mt-2 text-sm text-gray-400">
              EJS + routes propres : tu peux ajouter un panel admin, logs, stats, etc.
            </p>
          </div>
        </section>
      </main>
    </div>

    <%- include("../partials/footer") %>
  </body>
</html>