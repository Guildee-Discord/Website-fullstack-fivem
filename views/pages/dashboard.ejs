<!doctype html>
<html lang="fr">
  <head>
    <title>Dashboard — Website FiveM</title>
    <%- include("../partials/head") %>
  </head>
  <body class="bg-gray-100 min-h-screen">
    <%- include("../partials/nav", { user }) %>

    <main class="max-w-5xl mx-auto p-6">
      <div class="bg-white rounded-xl shadow p-6">
        <div class="flex items-center justify-between">
          <h1 class="text-2xl font-bold">Dashboard</h1>
          <a href="/logout" class="text-sm text-red-600 hover:underline">Déconnexion</a>
        </div>

        <p class="mt-3 text-gray-700">
          Ici tu affiches tout ce que tu veux (stats, boutique, whitelist, etc.). Pour l'instant on montre le lien Discord ↔ FiveM.
        </p>

        <div class="mt-6 flex items-center gap-4">
          <div class="w-14 h-14 rounded-full bg-gray-200 overflow-hidden flex items-center justify-center">
            <% if (user.avatar) { %>
              <img class="w-full h-full object-cover" src="https://cdn.discordapp.com/avatars/<%= user.discord_id %>/<%= user.avatar %>.png" />
            <% } else { %>
              <span class="text-gray-500">?</span>
            <% } %>
          </div>

          <div>
            <div class="font-semibold text-lg"><%= user.username %></div>
            <div class="text-xs text-gray-500">discord_id: <%= user.discord_id %></div>
            <div class="text-xs text-gray-500">identifier: <%= user.identifier %></div>
          </div>
        </div>
      </div>
    </main>

    <%- include("../partials/footer") %>
  </body>
</html>
