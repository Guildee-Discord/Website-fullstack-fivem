<nav class="navbar fixed top-0 left-0 right-0 z-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-20">
            <!-- Logo -->
            <div class="flex items-center space-x-3">
                <div class="relative w-14 h-14">
                    <div
                        class="absolute inset-0 bg-gradient-to-br from-cyan-400 to-purple-600 rounded-lg blur-sm opacity-75">
                    </div>
                    <div
                        class="relative w-14 h-14 bg-black rounded-lg border-2 border-cyan-400 flex items-center justify-center">
                        <span class="text-2xl font-black logo-text cyber-text">CR</span>
                    </div>
                </div>
                <div>
                    <h1 class="text-2xl font-black logo-text cyber-text tracking-wider">CITY-RP</h1>
                    <p class="text-xs text-cyan-400 tracking-widest">FIVEM SERVER</p>
                </div>
            </div>

            <!-- Desktop Menu -->
            <div class="hidden lg:flex items-center space-x-8">
                <a href="#home" class="nav-link text-sm font-semibold uppercase tracking-wider">Accueil</a>
                <a href="#features" class="nav-link text-sm font-semibold uppercase tracking-wider">Features</a>
                <a href="#jobs" class="nav-link text-sm font-semibold uppercase tracking-wider">Métiers</a>
                <a href="#store" class="nav-link text-sm font-semibold uppercase tracking-wider">Boutique</a>
                <a href="#rules" class="nav-link text-sm font-semibold uppercase tracking-wider">Règlement</a>

                <div class="flex items-center space-x-3">
                    <div
                        class="flex items-center space-x-2 px-4 py-2 rounded-lg bg-green-500/20 border border-green-500/50">
                        <div class="w-2 h-2 rounded-full bg-green-400 animate-pulse"></div>
                        <span class="text-xs font-bold text-green-400">243 EN LIGNE</span>
                    </div>

                    <% if (!user) { %>
                        <!-- Connexion -->
                        <a href="/connexion" class="cyber-btn px-6 py-3 rounded-lg inline-flex items-center gap-2">
                            <i class="fa-brands fa-discord"></i>
                            <span>Connexion</span>
                        </a>
                        <% } else { %>
                            <!-- User dropdown -->
                            <details class="relative">
                                <summary class="list-none cursor-pointer select-none">
                                    <div
                                        class="flex items-center gap-3 px-3 py-2 rounded-lg border border-cyan-400/30 bg-black/40 hover:bg-cyan-400/10 transition">
                                        <% const avatarUrl=user.avatar ? (user.avatar.startsWith("http") ? user.avatar :
                                            `https://cdn.discordapp.com/avatars/${user.discord_id}/${user.avatar}.png?size=64`)
                                            : null; %>
                                            <div
                                                class="h-10 w-10 rounded-lg overflow-hidden border border-cyan-400/30 bg-black grid place-items-center">
                                                <% if (avatarUrl) { %>
                                                    <img src="<%= avatarUrl %>" alt="Avatar"
                                                        class="h-full w-full object-cover">
                                                    <% } else { %>
                                                        <span class="font-black cyber-text">
                                                            <%= (user.username || "U" ).charAt(0).toUpperCase() %>
                                                        </span>
                                                        <% } %>
                                            </div>

                                            <div class="leading-tight hidden xl:block">
                                                <p class="text-sm font-bold text-white truncate max-w-[140px]">
                                                    <%= user.username || "Utilisateur" %>
                                                </p>
                                                <p class="text-[11px] text-cyan-300/80">
                                                    <%= user.linked ? "Compte lié" : "Non lié" %>
                                                </p>
                                            </div>

                                            <i class="fa-solid fa-chevron-down text-cyan-300 text-xs opacity-90"></i>
                                    </div>
                                </summary>

                                <div
                                    class="absolute right-0 mt-3 w-64 rounded-xl border border-cyan-400/30 bg-[#070716]/95 backdrop-blur shadow-[0_20px_60px_rgba(0,255,255,.15)] overflow-hidden">
                                    <div class="px-4 py-3 border-b border-cyan-400/20">
                                        <p class="text-sm font-bold text-white truncate">
                                            <%= user.username || "Utilisateur" %>
                                        </p>
                                        <p class="text-xs text-gray-400 truncate">Discord ID: <%= user.discord_id %>
                                        </p>
                                    </div>

                                    <nav class="p-2 text-sm">
                                        <a href="/dashboard"
                                            class="flex items-center gap-2 px-3 py-2 rounded-lg hover:bg-cyan-400/10 transition">
                                            <i class="fa-solid fa-chart-line text-cyan-300 w-5 text-center"></i>
                                            Dashboard
                                        </a>

                                        <% if (!user.linked) { %>
                                            <a href="/link"
                                                class="flex items-center gap-2 px-3 py-2 rounded-lg hover:bg-cyan-400/10 transition">
                                                <i class="fa-solid fa-link text-cyan-300 w-5 text-center"></i>
                                                Lier mon compte
                                            </a>
                                            <% } else { %>
                                                <form method="POST" action="/unlink" class="px-1">
                                                    <button type="submit"
                                                        class="w-full flex items-center gap-2 px-3 py-2 rounded-lg hover:bg-red-500/10 transition text-left"
                                                        onclick="return confirm('Délier ton compte FiveM ?');">
                                                        <i class="fa-solid fa-unlink text-red-300 w-5 text-center"></i>
                                                        Délier mon compte
                                                    </button>
                                                </form>
                                                <% } %>

                                                    <a href="/logout"
                                                        class="flex items-center gap-2 px-3 py-2 rounded-lg hover:bg-purple-500/10 transition text-purple-200">
                                                        <i
                                                            class="fa-solid fa-right-from-bracket text-purple-300 w-5 text-center"></i>
                                                        Déconnexion
                                                    </a>
                                    </nav>
                                </div>
                            </details>
                            <% } %>
                </div>
            </div>

            <!-- Mobile Menu Button -->
            <button id="mobile-menu-btn"
                class="lg:hidden p-2 rounded-lg border-2 border-cyan-400 hover:bg-cyan-400/10 transition">
                <svg class="w-6 h-6 text-cyan-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path id="menu-icon" stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                        d="M4 6h16M4 12h16M4 18h16"></path>
                    <path id="close-icon" class="hidden" stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                        d="M6 18L18 6M6 6l12 12"></path>
                </svg>
            </button>
        </div>
    </div>

    <!-- Mobile Menu -->
    <div id="mobile-menu" class="hidden mobile-menu lg:hidden">
        <div class="px-4 py-6 space-y-4">
            <a href="#home"
                class="block py-3 px-4 rounded-lg hover:bg-cyan-400/10 border border-transparent hover:border-cyan-400/30 transition">Accueil</a>
            <a href="#features"
                class="block py-3 px-4 rounded-lg hover:bg-cyan-400/10 border border-transparent hover:border-cyan-400/30 transition">Features</a>
            <a href="#jobs"
                class="block py-3 px-4 rounded-lg hover:bg-cyan-400/10 border border-transparent hover:border-cyan-400/30 transition">Métiers</a>
            <a href="#store"
                class="block py-3 px-4 rounded-lg hover:bg-cyan-400/10 border border-transparent hover:border-cyan-400/30 transition">Boutique</a>
            <a href="#rules"
                class="block py-3 px-4 rounded-lg hover:bg-cyan-400/10 border border-transparent hover:border-cyan-400/30 transition">Règlement</a>

            <div class="pt-4 space-y-3">
                <div
                    class="flex items-center justify-center space-x-2 px-4 py-3 rounded-lg bg-green-500/20 border border-green-500/50">
                    <div class="w-2 h-2 rounded-full bg-green-400 animate-pulse"></div>
                    <span class="text-sm font-bold text-green-400">243 JOUEURS EN LIGNE</span>
                </div>

                <% if (!user) { %>
                    <a href="/connexion"
                        class="cyber-btn w-full px-6 py-4 rounded-lg inline-flex items-center justify-center gap-2">
                        <i class="fa-brands fa-discord"></i>
                        <span>Connexion</span>
                    </a>
                    <% } else { %>
                        <a href="/dashboard"
                            class="cyber-btn w-full px-6 py-4 rounded-lg inline-flex items-center justify-center gap-2">
                            <i class="fa-solid fa-chart-line"></i>
                            <span>Dashboard</span>
                        </a>

                        <% if (!user.linked) { %>
                            <a href="/link"
                                class="w-full px-6 py-4 rounded-lg border-2 border-cyan-400 text-cyan-300 hover:bg-cyan-400/10 transition font-bold uppercase tracking-wider text-center block">
                                Lier mon compte
                            </a>
                            <% } else { %>
                                <form method="POST" action="/unlink">
                                    <button type="submit"
                                        class="w-full px-6 py-4 rounded-lg border-2 border-red-500 text-red-300 hover:bg-red-500/10 transition font-bold uppercase tracking-wider"
                                        onclick="return confirm('Délier ton compte FiveM ?');">
                                        Délier mon compte
                                    </button>
                                </form>
                                <% } %>

                                    <a href="/logout"
                                        class="w-full px-6 py-4 rounded-lg border-2 border-purple-500 text-purple-300 hover:bg-purple-500/10 transition font-bold uppercase tracking-wider text-center block">
                                        Déconnexion
                                    </a>
                                    <% } %>
            </div>
        </div>
    </div>
</nav>