<footer class="mt-10 border-t border-white/10 bg-gray-950/70 backdrop-blur">
  <div class="mx-auto w-[min(1200px,92vw)] px-4 sm:px-0 py-10">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">

      <!-- Brand -->
      <div>
        <div class="flex items-center gap-3">
          <span class="grid h-10 w-10 place-items-center rounded-xl bg-white/5 border border-white/10">
            <span class="text-indigo-300 font-black text-lg">F</span>
          </span>
          <div class="leading-tight">
            <div class="font-bold text-gray-100">Website FiveM</div>
            <div class="text-xs text-gray-400">Discord • MySQL • Dashboard</div>
          </div>
        </div>

        <p class="mt-4 text-sm text-gray-400 leading-relaxed">
          Un site web pensé pour ton serveur FiveM : login Discord, liaison utilisateur,
          et un dashboard prêt à évoluer. Sans casser la base du jeu.
        </p>

        <div class="mt-4 inline-flex items-center gap-2 rounded-xl border border-white/10 bg-white/5 px-3 py-2 text-xs text-gray-300">
          <span class="inline-block h-2 w-2 rounded-full bg-indigo-400"></span>
          <span>Theme sombre • UI “glass”</span>
        </div>
      </div>

      <!-- Links -->
      <div>
        <h3 class="text-sm font-semibold text-gray-100">Navigation</h3>
        <ul class="mt-4 space-y-2 text-sm">
          <li>
            <a href="/" class="text-gray-300 hover:text-white hover:underline">
              Accueil
            </a>
          </li>
          <li>
            <a href="/dashboard" class="text-gray-300 hover:text-white hover:underline">
              Dashboard
            </a>
          </li>
          <li>
            <a href="/link" class="text-gray-300 hover:text-white hover:underline">
              Lier mon compte
            </a>
          </li>
          <li>
            <% if (!user) { %>
              <a href="/login" class="text-indigo-300 hover:text-indigo-200 hover:underline">
                Se connecter (Discord)
              </a>
            <% } else { %>
              <a href="/logout" class="text-gray-300 hover:text-white hover:underline">
                Déconnexion
              </a>
            <% } %>
          </li>
        </ul>
      </div>

      <!-- Status / Actions -->
      <div>
        <h3 class="text-sm font-semibold text-gray-100">Statut</h3>

        <div class="mt-4 rounded-2xl border border-white/10 bg-white/5 p-4">
          <% if (!user) { %>
            <div class="text-sm text-gray-300">
              <span class="text-gray-400">Session :</span> Non connecté
            </div>
            <a href="/login"
               class="mt-4 inline-flex w-full items-center justify-center rounded-xl bg-indigo-600 px-4 py-2 font-semibold text-white hover:bg-indigo-500">
              Se connecter avec Discord
            </a>
          <% } else { %>
            <div class="flex items-center justify-between gap-3">
              <div class="min-w-0">
                <div class="text-sm font-semibold text-gray-100 truncate">
                  <%= user.username ? user.username : "Compte Discord" %>
                </div>
                <div class="text-xs text-gray-400 truncate">
                  discord_id: <%= user.discord_id %>
                </div>
              </div>

              <div class="shrink-0 inline-flex items-center gap-2 rounded-xl border border-white/10 bg-black/20 px-3 py-1.5 text-xs">
                <span class="inline-block h-2.5 w-2.5 rounded-full <%= user.linked ? 'bg-emerald-400' : 'bg-yellow-400' %>"></span>
                <span class="text-gray-200"><%= user.linked ? 'Lié' : 'Non lié' %></span>
              </div>
            </div>

            <div class="mt-4 grid grid-cols-1 sm:grid-cols-2 gap-3">
              <a href="/dashboard"
                 class="inline-flex items-center justify-center rounded-xl border border-white/10 bg-black/20 px-4 py-2 text-gray-100 hover:bg-white/5">
                Dashboard
              </a>

              <% if (user.linked) { %>
                <form method="POST" action="/unlink">
                  <button type="submit"
                          class="w-full inline-flex items-center justify-center rounded-xl bg-red-600 px-4 py-2 font-semibold text-white hover:bg-red-500"
                          onclick="return confirm('Es-tu sûr de vouloir délier ton compte ?');">
                    Délier
                  </button>
                </form>
              <% } else { %>
                <a href="/link"
                   class="inline-flex items-center justify-center rounded-xl bg-indigo-600 px-4 py-2 font-semibold text-white hover:bg-indigo-500">
                  Lier mon compte
                </a>
              <% } %>
            </div>
          <% } %>
        </div>

        <p class="mt-3 text-xs text-gray-500">
          Conseil : en prod, remplace la saisie manuelle par un code de liaison généré en jeu.
        </p>
      </div>
    </div>

    <!-- Bottom bar -->
    <div class="mt-10 flex flex-col sm:flex-row items-start sm:items-center justify-between gap-3 border-t border-white/10 pt-6 text-xs text-gray-500">
      <div>
        © <%= new Date().getFullYear() %> Website FiveM — Tous droits réservés.
      </div>
      <div class="flex items-center gap-3">
        <span class="rounded-lg border border-white/10 bg-white/5 px-2 py-1">Express</span>
        <span class="rounded-lg border border-white/10 bg-white/5 px-2 py-1">EJS</span>
        <span class="rounded-lg border border-white/10 bg-white/5 px-2 py-1">MySQL</span>
        <span class="rounded-lg border border-white/10 bg-white/5 px-2 py-1">Discord OAuth</span>
      </div>
    </div>
  </div>
</footer>
